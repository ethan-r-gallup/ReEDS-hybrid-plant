# Storage-hybrid debug tracker

Living tracker for storage-hybrid bugs: status, evidence, and validation runs.
Last updated: 2026-07-25 (round-4 active-set fixes applied — SH-33/35/36/39/41/42/43/44/45
FIXED and validated in nlval7 GREEN: all optimal, checker 33/33, NEUE 26.7 ppm.
Remaining open: SH-34/37/38/40 latents + SH-46/47 hygiene/nit groups).

**2026-07-24 fix pass (validated in nlval6_mk_boak_lo):**
- SH-11 FIXED: CSP carve-out restored in base VOM (c_supplyobjective.gms:168) + gen_objcoef replica (e_report.gms).
- SH-12 FIXED: `ramprate(wrapper)` = storage component's (b_inputs.gms ~5036); dead `ramprate_storage_hybrid` param removed; reserve_frac follows.
- SH-29 FIXED: `storage_eff(wrapper)` = plant-charging efficiency (reg-loss terms no longer bill standalone TES RTE).
- SH-30 FIXED: eq_storage_opres credits reactor headroom (avail·CAP − GEN_PLANT) as reserve backing for dispatchable wrappers.
- SH-31 FIXED per user rule: TES takes CSP's cost_opres (reg 3.24 $/MWh); wrappers take storage component's cost unconditionally. 3.24-vs-0 question RESOLVED 2026-07-24: user confirmed CSP's value is the physical choice — reg cost = wear from jogging the steam power block off the hot tank (same hardware as CSP); 0 is the inverter/battery answer, 10.13 would be ramping the reactor itself.
- SH-32 FIXED (user: both gross): PTC and REC/CES credited on GEN_PLANT for wrappers; TES plant-charge eff 0.99 → 0.999.
- SH-10 FIXED: `reg_cap_cost_diff(wrapper,r)` inherited from gen tech in GAMS (b_inputs, next to financing_risk_mult); cloner skips reg_cap_cost_diff*/incentives* (incl. root names; fixed `ref_`→`reg_` skip-list typo that let the wide file be column-cloned). Post-launch discovery: the wide run-copy is actually named `regional_cap_cost_diff.csv` — added to the skip list after nlval6 launched, so nlval6's CSV still carries the 528 legacy nuclear-wrapper rows; harmless because the GAMS inheritance overwrites/fills all 8 wrappers (verify SMR-wrapper parity in the checklist).
- SH-28 FIXED: `tc_phaseout_mult_t(wrapper)` inherited from gen tech each solve year (d1_financials.gms).
- SH-13 FIXED: dead cap_hyb block removed from capacity_credit.py (with explanatory note).
- SH-14 FIXED: missing hybrid_config row now a hard `error()` in process_storage_hybrids (was silent @error+continue).
- SH-15 FIXED: checker demo check maps wrapper prescriptions through wrapper_gentech when pooling is on.
- nlval6 config (user): cases_nlvaltest5.csv = moderate TES, mckinsey mandate as FLOOR (GSw_NuclearCapMandate=1), demo plants on.

Legend: **FIXED** = implemented + validated in a run · **OPEN** = confirmed, not yet fixed ·
**DECISION** = needs a design call before fixing · **LATENT** = inactive in current
nuclear/TES configs, will bite future configs · **NO-OP** = code present but ineffective.

---

## 1. Fixed and validated

| ID | Bug | Fix | Validated |
|----|-----|-----|-----------|
| SH-01 | 100% CF: `eq_hybrid_plant_energy_limit` capped dispatchable plant at raw CAP without `avail` | `avail(i,r,h)*CAP` term (c_supplymodel.gms ~3462-3477) | nlval4: wrapper CF 0.85-0.90, no 0.999 |
| SH-02 | TES financing multiplier = 0: tes_ms missing from techs/ivt/tech-subset-table keying gates + all financials CSVs (lost in nuclear-stor merge) | Restored from upstream ReEDS-2.0: techs_default, ivt_*, TES column in tech-subset-table + i_subtech, financials_tech_*, construction_times, cap_penalty, incentives_*, reg_cap_cost_diff `TES\|CSP` header, degradation. Minimal scope: tes_ms NOT standalone-buildable | nlval4/5: ccmult(tes_ms)=1.0247, risk mult ~0.98, TES capex billed; standalone tes_ms cap = 0 |
| SH-03 | PRAS modeled wrappers as full-CAP grid-charged Batteries (reactor invisible to adequacy) | GeneratorStorage split: prep_data writes `hybrid_config_{t}.csv`; reeds2pras `process_storage_hybrids` (inflow=CAP, chg=dis=bcr·CAP, energy=CAP_ENERGY, grid_inj=(1+bcr)·CAP, chg_eff 0.99); 5-frame `split_generator_types`; keyed energy dict in `process_storages` | nlval5 .pras: 19 wrapper GeneratorStorages, Natrium p21 exact (345/172/1035/518), 0 wrappers in storages/generators |
| SH-04 | Outage files battery-first: wrappers carried FOR 0.02/SOR 0.006/MTTR 24 | Gen-first precedence in copy_files for outage_forced/scheduled_static, mttr, unitsize(_atb) | nlval5 .pras λ/μ → FOR 0.03, MTTR 298 (reactor values) |
| SH-05 | `eq_plant_capacity_limit` `+STORAGE_IN_PLANT` double-counted charging (2 MW envelope per MW charged); anti-simultaneity already enforced twice on storage side | Sign flip to `-STORAGE_IN_PLANT` | nlval4/5 optimal; envelope no longer binds spuriously |
| SH-06 | TES wrappers: free/unbacked opres (cost_opres overwritten to 0), zero cc_storage, minduration double-stack (~4x forced TES energy) | cost_opres nonzero-stortech guard; `cc_storage$storage_hybrid=1`; `mintesduration$[tes$(not storage_hybrid)]` | nlval4/5: cap_firm wrapper rows populated (hot = (1+bcr)·CAP); durations 4-9h not forced ~2.75h |
| SH-07 | Policy/lifecycle gaps: state nuclear ban bypass, CES ineligibility, no endogenous retirement (incl. nuclear protections), hybrid_cc_derate=0 for VRE wrappers, winter_cap_ratio missing | b_inputs extensions keyed on `storage_hybrid_gentech` | nlval4/5: winter uprate visible in cap_firm cold |
| SH-08 | Reporting: stor_energy_cap=0, stor_inout booked total gen as discharge, emissions on net GEN, LCOE energy multiplier | e_report/e_powfrac fixes (CAP_ENERGY-based, GEN_STORAGE-based, GEN_PLANT emissions, `_s` multiplier) | nlval5: in/out ratio = 0.9900 = charge_eff; storage loss accounting closes to the MWh; 0 wrapper emit rows |
| SH-09 | Power-only prescription of a storage-hybrid infeasible: `eq_forceprescription_energy` forced INV_ENERGY=0 pre-firstyear (no MWh prescribed, EXTRA_PRESCRIP_ENERGY gated on firstyear_pcat) vs `eq_battery_minduration` floor. Found via Natrium demo → CPLEX conflict refiner (5-row minimal conflict) | c_supplymodel.gms ~972-980: EXTRA_PRESCRIP_ENERGY slack also active when a nonzero cumulative POWER prescription exists at (pcat,r,t) — MW exogenous, MWh endogenous | nlval5: 2032 solved straight optimal; Natrium built 345 MW + 1035 MWh (6h, 4x the floor, chosen voluntarily) |

## 2. Open findings — active in current runs (from 2026-07-24 final review)

| ID | Sev | Bug | Where | Impact / failure scenario | Status |
|----|-----|-----|-------|---------------------------|--------|
| SH-10 | HIGH | SMR wrappers get `reg_cap_cost_diff = 0`: propagation matches literal tech-name headers, but the file is keyed by tech GROUPS (`NUCLEAR`, `TES\|CSP`); "Nuclear" wrappers match by name-luck, `nuclear-smr` wrappers match nothing | copy_files.py:2251-2264; verified nlval5 inputs_case/reg_cap_cost_diff.csv (528 wrapper rows, none for storage-hybrid-1..4-nuclear-smr; base Nuclear-SMR has 132 via group expansion) | SMR wrappers escape regional capital-cost adders standalone SMR pays (e.g. +10.8% at p1) → hybrid-vs-standalone economics tilted pro-hybrid. Affects nlval5's 5.4 GW endogenous hybrid result and all NREL sweeps | **FIXED 2026-07-24, nlval6-validated** |
| SH-11 | HIGH | CSP variable O&M free in objective: base VOM excludes all `hybrid_plant(i)`; re-add terms cover only pvb + storage_hybrid, never CSP (carve-out lost in `hybrid_plant` rename) | c_supplyobjective.gms:168 (re-adds 172-185); e_report.gms:1469 still bills it → objective/report disagree; e_report.gms:2084 replicates omission | Any run with CSP (incl. existing fleet) dispatches CSP with free VOM | **FIXED 2026-07-24, nlval6-validated** (carve-out restored in objective + gen_objcoef) |
| SH-12 | MED | Wrapper TES cannot provide opres: `ramprate(wrapper)` = gen tech's → nuclear's tiny `reserve_frac` → `eq_ORCap_small_res_frac` caps OPRES ~0. `ramprate_storage_hybrid` (storage-side) is defined but NEVER referenced (dead code). Defeats the deliberate storage-side cost_opres + eq_storage_opres work | b_inputs.gms:5036 (gen inherit), 5038-5039 (dead param), 5047 (reserve_frac); confirmed in nlval5 outputs: 0 wrapper opres rows | TES-backed reserves impossible; opres value of hybrids invisible to the optimizer | **FIXED 2026-07-24, nlval6-validated**: `ramprate(wrapper)` from `storage_hybrid_stortech` (design: whole plant takes storage's flexibility); dead param removed; nlval6 wrappers provide combo reserves (config-2: 1036 MWh, config-4: 1063 MWh at 2035) |
| SH-13 | MED | `capacity_credit.py` cap_hyb block (added 2026-07-23) is a NO-OP: feeds cap_stor/cap_stor_agg but only consumers are an `rte` column never created (try→`cc_default_rte`) and unused `cap_stor_ccreg`; sdbin sizes come entirely from peak-reduction geometry | ReEDS_Augur/capacity_credit.py:138-152, 264-277 | Intended sdbin correction for wrapper TES never happens (harmless otherwise) | **FIXED 2026-07-24**: block removed (verified no-op) |
| SH-14 | MED | Missing hybrid_config row silently drops the ENTIRE plant (reactor included) from PRAS: `@error` doesn't throw in Julia; `continue` skips. Realistic trigger: prep_data falls back to empty tech list if gdx lacks `storage_hybrid` symbol → header-only config while tech-subset-table still routes wrappers to storage_hybrid_capacity | reeds2pras/src/utils/reeds_data_parsing.jl:922-928 | Adequacy silently loses GW-scale capacity | **FIXED 2026-07-24**: hard error() |
| SH-15 | LOW-MED | Learning checker demo check (5) filters to base techs only — Natrium wrapper's 345 MW excluded from `expected`; check passes silently if wrapper-prescription experience ever fails to record | nuclear_learning.py:542 | Check can't catch the failure it exists for (engine itself correct: wrappers in `nuclear_learning_exptech`) | **FIXED 2026-07-24**: mapped when pooling on |

## 2b. Directional scan 2026-07-24: errors DISADVANTAGING nuclear/SMR wrappers

Scan question: where do wrappers pay more, earn less, or face tighter constraints than
standalone nuclear/SMR + equivalent storage? (Advantaging bugs already in §2.)

| ID | Sev | Bug | Where | Standalone comparison / failure scenario | Status |
|----|-----|-----|-------|------------------------------------------|--------|
| SH-28 | MED (latent) | SMR wrappers got NO incentive rows → `safe_harbor=0` → tax credits phase out up to 6 years earlier. Same group-label root cause as SH-10: the cloner skips `incentives_*.csv` by name but the run-copy is `incentives.csv` (escapes skip); "Nuclear" literally matches the NUCLEAR group row (wrappers 5-8 cloned, safe_harbor 6) while "Nuclear-SMR"/tes_ms match nothing (wrappers 1-4 empty). Verified nlval5: `safe_harbor.csv` SMR wrappers 0.0 vs Nuclear-SMR 6.0. ITC path repaired in GAMS (d1_financials rebuilds from components) but the PTC term (c_supplyobjective.gms:374-378) uses the wrapper's OWN `tc_phaseout_mult` — never overridden from gen tech | copy_files.py:2090-2098 (skip list), reeds/financials.py:230-243, tc_phaseout.py:112-185, b_inputs.gms:1639 | Zero realized impact in OBBBA cases (nuclear PTC=0, ITC-only) but wrong under any nuclear-PTC incentive suffix (IRA 45Y); also makes cloning of incentive files nondeterministic | **FIXED 2026-07-24**: GAMS tc_phaseout_mult_t inheritance + cloner skips incentives; nlval6-validated |
| SH-29 | LOW-MED | Reg-reserve energy losses billed at TES round-trip RTE (0.55 advanced / 0.47 moderate) on ALL wrapper reserves: `storage_eff(wrapper)` = tes_ms rte (b_inputs.gms:6002) feeds the `(1-storage_eff)/2·reg_energy_frac` terms, while the wrapper's actual modeled charge efficiency is 0.99 (b_inputs.gms:6018). Wrapper pays (1-0.55)/2 = 0.225/MW — 3x a battery's charge; standalone nuclear pays zero | c_supplymodel.gms:3202-3207, 3228-3231; b_inputs.gms:6002 vs 6018 | Wrapper reg reserves overpriced vs both parents; currently masked by SH-12 (reserves ~0) but binds as soon as SH-12 is fixed | **FIXED 2026-07-24**: storage_eff(wrapper)=plant-charging eff (0.999) |
| SH-30 | LOW-MED | ALL wrapper reserves must be pre-stored in TES: eq_storage_opres requires STORAGE_LEVEL ≥ hours×(total OPRES) with a single OPRES variable per wrapper — reactor-headroom reserves are conflated with storage reserves. Violates the 2026-07-24 design principle's own asymmetry: the generator can't run out of energy, so reactor headroom (avail·CAP − GEN_PLANT) should count as reserve backing without stored energy | c_supplymodel.gms:3213-3237 | Standalone nuclear provides reserves with no energy backing; nuclear+battery pair only backs the battery share; wrapper must bank TES energy for all of it | **FIXED 2026-07-24**: eq_storage_opres credits avail·CAP−GEN_PLANT for dispatchable wrappers |
| SH-31 | LOW | Wrapper pays nuclear's reg cost (10.13 $/MWh) on ALL reserves incl. TES-provided ones: tes_ms has no cost_opres row so the SH-06 nonzero guard (correctly) keeps the gen value — but a standalone pair pays 10.13 only on the nuclear share; battery-provided reg is free | inputs_case cost_opres_default.csv; b_inputs.gms:5074-5075; c_supplyobjective.gms:233-234 | Small standing cost bias against wrapper reserves | **FIXED 2026-07-24** (user rule): TES takes CSP cost_opres (reg 3.24); wrappers take storage cost unconditionally. 3.24 confirmed physically correct (turbine-cycling wear, not reactor ramping, not inverter-free) — flag closed |
| SH-32 | LOW | PTC and CES credited on NET GEN — wrapper loses credit on the 1% TES charging loss; standalone nuclear is credited on gross output regardless of a separately-metered battery | c_supplymodel.gms:3450-3462 (eq_plant_total_gen); c_supplyobjective.gms:374-378 | ~1% of cycled energy; nil in nlval5 (no nuclear PTC); CES effect only where CES binds | **FIXED 2026-07-24** (user: both gross; PTC+REC on GEN_PLANT), nlval6-validated |

Design observations (deliberate code, but wrapper pays more than the sum of its parts — flag to NREL):
- **Capex premium widens with nuclear learning**: wrapper OCC = learned-gen-OCC·(1−powerblock share) + (1+bcr)·TES-island cost (b_inputs.gms:6144-6147; verified exact in nlval5 cost_cap: wrapper-1 = 3,753,194 vs standalone SMR+0.25·tes = 3,507,765, +7.0%). The TES island doesn't learn with nuclear, so the hybrid premium grows every learning step. Confirm intended under GSw_NuclearLearning.
- **TES share financed at nuclear risk**: storage-side fin mult scaled by gen/storage financing-risk ratio ≈ +5.8% vs standalone tes_ms (d1_financials.gms:166-174). Deliberate (whole-plant financing) but systematic.
- **Min TES duration 1.5h per MW storage power** vs battery_li's 0.2h (7.5x forced energy vs a nuclear+battery comparator; looser than standalone TES's 2h).

Directional scan verified clean: cost_fom = gen + bcr·tes + fom_energy (matches standalone pair); VOM structure = pvb pattern, no double-billing; fuel/heat-rate/emissions on gen basis (b_inputs.gms:4883 heat-rate override); fin-mult blend weights = capex composition weights; maxage 80yr via GAMS override; retirement protections mirror nuclear; wrappers in nuclear tg only (growth/queue parity at reactor MW); gen side gets full winter uprate in PRM; sdbin chain has no wrapper-zeroing term — **cold-season zero storage credit for config-4 is system-wide duration-bin geometry (standalone batteries got 0.000 cold firm credit in every checked BA; longer-duration wrapper TES actually out-earns batteries: p13 cold bins 6/12/24 populated for wrapper, none for battery)**; interday-linkage equations handle CAP_ENERGY correctly if ever enabled.

## 2c. Round-4 final sweep, 2026-07-24 (4 parallel lenses; all findings below independently re-verified against code/outputs before entry)

Top-severity findings first. "Active" = affects current nuclear/TES nlval-style runs.

| ID | Sev | Bug | Where | Impact / failure scenario | Status |
|----|-----|-----|-------|---------------------------|--------|
| SH-33 | HIGH (latent — GSw_WaterMain=1) | Cooling-water link file corrupted by the cloner: it substituted wrapper names into the `ii` PARENT column of `i_coolingtech_watersource_link.csv` (verified nlval6: 224 rows like `nuclear-SMR_o_fsu,storage-hybrid-1-nuclear-smr,o,fsu`; 5 "parents" per nuclear cooling variant). Under WaterMain: (a) wrappers become `i_numeraire` → `valcap=no` (b_inputs.gms:2706) with no cooling variants to replace them — entire hybrid feature silently vanishes; (b) every `sum{ii$ctt_i_ii(i,ii),…}` inheritance for STANDALONE nuclear cooling variants multi-counts ~5x (maxage 400, firstyear ~10,000, capcost ~5-6x) — standalone nuclear corrupted too | copy_files.py (file not in skip list); b_inputs.gms:339-367, 1679, 1916, 4449-4457 | Any water-constrained NREL run: no hybrids + nuclear priced off the board, no error raised | **FIXED 2026-07-25** (both link files in skip list; nlval7 cooling-link 0 wrapper rows). Only file affected (scanned all nlval6 CSVs for wrapper names in non-first columns) |
| SH-34 | MED (latent — GSw_WaterMain=1) | Wrappers bypass ALL water machinery even after SH-33: in neither `i_water_cooling` nor `i_water_nocooling`, so zero water_rate, no eq_water_accounting/capacity/use_limit, no water access cost — reactor cools for free while standalone nuclear pays and is constrained. Note eq_water_accounting keys net GEN (wrong basis if wrappers ever added) | b_inputs.gms:304-353, 1218; c_supplymodel.gms:3616-3667; c_supplyobjective.gms:83-89 | Pure pro-hybrid arbitrage under water constraints | OPEN — give wrappers i_water_nocooling + gen water_rate via storage_hybrid_gentech, or abort on WaterMain∧HybridPlant |
| SH-35 | MED (ACTIVE — Sw_OpRes on) | Wrapper reserves capped by NET grid injection: `eq_ORCap_small/large_res_frac` exclude only storage_standalone/hyd_add_pump. With storage ramprate (SH-12), reg reserve_frac=0.5 → small branch: net GEN(h) ≥ OPRES(reg,h) hour-by-hour — overnight (TES charging, net GEN≈0) wrapper can sell ZERO reg even with full tank + reactor headroom; standalone batteries exempt entirely. Explains nlval6 reserves being combo-only (combo's longer ramptime → large branch anchored to max-load timeslice, loose) | c_supplymodel.gms:1540-1572 | Understates hybrid reserve value in every Sw_OpRes run; violates storage-flexibility principle | **FIXED 2026-07-25, nlval7-validated** (`$(not storage_hybrid(i)$Sw_HybridPlant)` on both eq_ORCap_*; precedence of unparenthesized idiom empirically verified in GAMS 50; wrapper opres +7%) |
| SH-39 | MED (ACTIVE — PRAS stress iterations) | Wrapper TES SOC invisible to stress-period selection + crash: run_pras.jl requests only `PRAS.StorageEnergy()` and writes `-energy.h5` from `sys.storages.names` — GeneratorStorages (post-SH-03 wrappers) excluded, so `GSw_PRM_StressStorageCutoff` shoulder-period logic sees only standalone-battery SOC (regression introduced by our own SH-03 split). CRASH sub-case: zero standalone storages → empty h5 → `dfenergy.index = timeindex` (stress_periods.py:210) ValueError BEFORE the `.empty` guard at :136, killing every solve-year iteration | run_pras.jl:182-184, 288-303; stress_periods.py:204-217 | Wrapper-heavy regions mis-select shoulder stress periods; all-wrapper-storage system crashes | **FIXED 2026-07-25, nlval7-validated** (GeneratorStorageEnergy requested; genstor columns filtered by `occursin("\|")` — admits wrappers, excludes hydro; empty-frame guard in stress_periods; nlval7 -energy.h5 has 3 wrapper columns) |
| SH-40 | MED (latent — armed by the SH-27 fix) | Gen_Storage applies scheduled-outage derate to the WRONG quantities: `(1−SOR)` hits only grid_inj_cap (and energy_cap in the storage getter) — `inflow` (reactor) underated. grid_inj=(1+bcr)·CAP so derated envelope stays above full reactor output → hybridized reactor loses ~nothing to maintenance in PRAS while standalone reactor loses full SOR share. Inactive today ONLY because wrappers lack scheduled-outage rows (=SH-27); fixing SH-27 alone would silently arm this pro-hybrid bias — FIX TOGETHER. File's own TODO at :153 flags it | reeds2pras/src/models/Gen_Storage.jl:153-166 (verified: get_inflow no derate, get_grid_injection_capacity ×(1−SOR)) | Pro-hybrid adequacy bias the moment wrapper SOR>0 | **FIXED 2026-07-25** (landed together with SH-27 as required): Gen_Storage getters scope by `is_storage_hybrid` — wrappers derate ALL power-side capabilities (inflow/charge/discharge/grid_withdrawal/grid_inj) by (1−SOR) with the tank UNDERATED; non-wrapper genstors (reservoir hydro, whose inflow is water that keeps flowing during turbine maintenance) keep prior behavior bit-identically. Resolves the file's own TODO |
| SH-41 | MED (ACTIVE — any pvb run; reporting only) | pvb VOM missing from systemcost: objective bills `cost_vom_pvb_p·GEN_PLANT + cost_vom_pvb_b·GEN_STORAGE` (c_supplyobjective.gms:174-179) but `cost_vom_pvb` has ZERO hits in e_report — the storage_hybrid rewrite of op_vom_costs (:1467-1483) dropped the upstream pvb terms | e_report.gms:1467-1483 | pvb VOM absent from systemcost_techba; error_check('z') drifts by full pvb VOM | **FIXED 2026-07-25** (both pvb terms re-added; adversarial review caught + fixed a would-be double-count: base VOM guard widened to the objective's `not hybrid_plant$(not csp)` so pvb's placeholder cost_vom is excluded) |
| SH-42 | MED (ACTIVE — reporting) | Energy-FOM reporting: (a) report guard at e_report.gms:1492 is `$storage_hybrid(i)` only while objective bills `battery∨tes∨storage_hybrid` (c_supplyobjective.gms:194-195) — standalone battery/tes energy FOM never reaches op_fom_costs (multi-vintage, so one_newv branch doesn't catch); (b) `cap_energy_ivrt` never assigned in e_report for battery/tes/wrapper (only psh at :951) — wrapper values survive only via d3_data_dump workfile leftovers with `$trange` excluding first model year | e_report.gms:1488-1500, 951 | Reported FOM understates LP; silent dependence on include-order accident | **FIXED 2026-07-25, nlval7-validated** (guard widened to battery/tes/storage_hybrid; cap_energy_ivrt assigned from CAP_ENERGY.l in e_report; one_newv branch verified mutually exclusive) |
| SH-43 | MED (ACTIVE — postprocessing) | Wrapper ITC invisible to retail-rate module: `itc_fractions.csv` has ZERO storage-hybrid rows (verified nlval6; base Nuclear-SMR rows at 0.3) — retail_rate_calculations merges capex on `i` with how='inner' → wrapper capex earns 0% ITC in retail rates + federal-tax-expenditure postprocessing while the LP applied ~31.7% | reeds/financials.py (itc_fractions writer); retail_rate_module/retail_rate_calculations.py ~:1785 | Retail rates overstate wrapper cost recovery; ITC expenditure understated by entire wrapper ITC | **FIXED 2026-07-25, nlval7-validated** (wrapper rows cloned from gen tech via storage_hybrid_gentechs.csv — 112 rows in nlval7; documented approximation: exact for reactor capex share, slight understatement vs true blend, 29.9% vs ~31.7%) |
| SH-44 | LOW-MED (latent until nuclear-PTC suffix) | PTC REPORTING on net GEN while LP (post-SH-32) credits GEN_PLANT: op_ptc_payments_negative (e_report.gms:1587-1590), tax_expenditure_ptc (:1824), costnew cost_ptc (:1225-1228) all use GEN.l; worse, gen_objcoef (:2107-2109) subtracts the FULL PTC from wrapper GEN's coefficient with no storage_hybrid guard (fuel term 2 blocks up has one) — wrong by whole PTC, not just losses | e_report.gms:1587-1590, 1824, 1225-1228, 2107-2109 | error_check('z') drift + wrong diagnostics under IRA-45Y-style cases | **FIXED 2026-07-25** (op_ptc_payments_negative + costnew mirror the objective incl. pvb STORAGE_IN_GRID netting; gen_objcoef guarded; tax_expenditure_ptc inherits via the systemcost row. Residual: retail-rate ITC-PTC_expenditures.py still computes PTC from net gen — pre-existing, ticketed under SH-47) |
| SH-45 | MED (mechanism) + DECISION | Skip-list path mismatch (3rd occurrence of the SH-10 class): entries use source-repo paths (`emission_constraints/emitrate.csv`, `financials/cap_penalty.csv`, `national_generation/gbin_min.csv`, `sets/tg.csv`) but runfiles flattens run-copies to basenames → never match. VERIFIED: nlval6 emitrate.csv contains cloned `storage-hybrid-5..8-nuclear` upstream-CH4/N2O rows despite the "never cloned" comment. No double-count (no GAMS-side emit inheritance) and clone = standalone-nuclear parity — arguably CORRECT under CO2e accounting (Sw_AnnualCap=2), contradicting §1's "0 wrapper emit rows" claim (that run had no CO2e). cap_penalty/gbin_min/tg are group-keyed → clone no-ops today | copy_files.py:2170-2182, match at :2309 | If skip paths ever "fixed" naively, wrapper upstream emissions silently drop to 0 under CO2e caps (pro-hybrid tilt) | **FIXED 2026-07-25** (USER DECISION: clone blessed — standalone parity; reviewer confirmed eq_emit_accounting bills hybrids on GEN_PLANT so parity holds. Skip list normalized to basenames with documented rationale; basename `tg.csv` also closes SH-46a's root-tg heuristic hazard; nlval7: emitrate 4 blessed rows, tg/cap_penalty clean) |
| SH-36 | LOW | sdbin bin-ordering penalty: wrappers exempt (obj guard `not thermal_storage` — fork widened from upstream's `not csp`) while their CAP_SDBIN is live; AND error_check('z') replica still uses `not csp` → spurious z-drift whenever wrappers hold sdbin capacity. Both found independently by 2 lenses. Inactive now (PRAS runs: Sw_PRM_CapCredit=0) | c_supplyobjective.gms:123 vs e_report.gms:1847 | Degenerate bin choice + free tie-break edge vs batteries under CapCredit | **FIXED 2026-07-25** (objective guard now `not csp`, token-identical to the replica; also newly excludes csp itself, matching eq_cap_sdbin_balance — reviewer verified csp was NOT in THERMAL_STORAGE so the old guard never excluded it) |
| SH-37 | LOW (latent — intertemporal solves) | cc_int/cc_excess PRM terms keyed `vre∨storage` include wrappers → would stack with the fork-added full-CAP gen-side term + CAP_SDBIN term. Sequential solves: cc_int=0, no impact | c_supplymodel.gms:1775-1798 | Firm-capacity double-count in intertemporal mode | OPEN — `$(not storage_hybrid(i))` on cc_int/cc_excess sums |
| SH-38 | LOW (latent, structural) | Wrapper CAP_ENERGY can only degrade at the GEN tech's rate (`degrade(i,tt,t)·INV_ENERGY` in eq_cap_energy_new_noret; degrade_annual(wrapper)=gen's). No-op today (nuclear/tes_ms/battery_li all 0.0) | c_supplymodel.gms:735; b_inputs.gms:2455 | Wrong TES degradation if storage degradation ever nonzero | OPEN — storage-side degrade via storage_hybrid_stortech on the INV_ENERGY term |
| SH-46 | LOW (group) | Cloner hygiene batch: (a) root `tg.csv` protected from tech-column detection only by the <60% canon-match heuristic — a shifted tg/i ratio would append 8 wrapper names into the TECH-GROUP set (growth limits/queues corrupted); (b) `construction_times.csv`/`financials_tech.csv` still cloned by name-luck (Nuclear/Nuclear-SMR literal matches; tes_ms never matches TES label) — currently 100% superseded in GAMS but exactly the SH-10/28 nondeterminism class; (c) `firstyear.csv`(wrapper=2025 via tes_ms!)/`years_until_endogenous.csv`(0)/`maxage.csv`(30) carry storage-first values that GAMS overrides — CSVs lie to any future Python consumer; belong in gen_first_files; (d) file-level idempotency scan (any `storage-hybrid` substring suppresses cloning for ALL 8 configs) — per-wrapper granularity already enforced downstream; (e) two-tech-column files (upgrade_link, ccs_link) would cross-clone fabricated link rows on first fossil pairing | copy_files.py:2258-2268, 2286-2295, 2363-2388; verified in nlval6 CSVs | Silent future breakage; misleading run archives | OPEN — skip-list additions + gen_first_files additions (firstyear, years_until_endogenous, maxage) |
| SH-47 | LOW (group) | Reporting/display nits: (a) `load_cat('stor_charge')` includes STORAGE_IN_PLANT (never grid load) → BA load-cat overstated by plant-charge MWh (e_report.gms:2265 via :691-692); (b) `revenue('rps')` on net GEN vs RECs earned on GEN_PLANT (:1019-1021, ~0.1%); (c) reqt_quant state_rps replica gated on Sw_HybridPlant vs model's Sw_StorageHybrid (:400 vs c_supplymodel.gms:2746); (d) diagnostic `lcoe`/`_nopol`/`_fullpol` omit INV_ENERGY capex for all CAP_ENERGY techs (:173-243; lcoe_built/pieces correct); (e) bokehpivot tech_map/style have no storage-hybrid rows → hot-pink fallback, never aggregates into Nuclear categories; (f) Augur diagnostic_plots `i.split('_')[0]` leaves wrapper names unmapped; (g) genstor builder silently falls back to static FOR where other paths @info-log (reeds_data_parsing.jl:940-944); (h) latent CCS trio: capture T&S cost, 45Q, emit_captured all on net GEN (companion to SH-18); (i) latent: gas-paired wrapper escapes ng_crf_penalty (captured at d1_financials:156 before the :218-221 multiply — nuke-ban mult at :125-131 is correctly ordered) | various (see per-item) | Cosmetic to ~0.1% each; (h)/(i) fold into SH-18 fossil ticket | OPEN except **(e) FIXED 2026-07-25** (tech_map `storage-hybrid*,Nuclear-TES Hybrid` + tech_style `#FF8C00,p` after Nuclear-SMR) — rest batch with next reporting pass |

Round-4 additional design observations (deliberate, flag to NREL):
- **Growth penalty basis**: cost_growth(wrapper) = full wrapper capex incl. TES → per-reactor-MW growth penalties higher for wrappers than standalone nuclear in the same tg (same pattern as upstream pvb; likely intended).
- **eq_national_gen (Sw_GenMandate)**: wrapper counts net GEN; storage-loss add-back is storage_standalone-only (~0.1% vs SH-32's "gross" decision).
- **eq_min_cf**: wrapper minCF=0.06 rows exist but net-GEN basis is directionally consistent with storage-flexibility (off by default; Sw_NukeFlex override correctly skips wrappers).

Round-4 verified clean (highlights): retirement/maxage parity (MWh retires with MW at maxage; SH-17 endogenous gap stands); refurb/upgrade sets exclude wrappers; growth tg inheritance + queue parity; uranium fuel2tech + GEN_PLANT fuel billing; opres 3.24 chain end-to-end; sdbin machinery (bcr·CAP basis); d3_data_dump symbol completeness + net/gross consistency; prep_data flow (devint, max_cap, energy self-filter, storcap-cutoff wrapper protection, no net-load double-count); capacity_credit cc_old/cc_mar re-gated VRE-only in GAMS (no wrapper zero/double-count); no osprey/dispatch module exists to mis-bucket wrappers; reeds2pras name handling (no `\|` in wrapper names, no regex mangling of hyphen-digit names anywhere in Augur/reeds/*.py); NEUE/EUE consumption region-keyed only; nuclear_learning.py experience=INV.l + OCC recomposition identical b_inputs↔d_solveoneyear (no TES double-count); pvf discounting parity; e_powfrac STORAGE_IN_GRID-only load; objective capex single-billing re-verified incl. INV_ENER_UP; blend weights = capex composition exactly; propagation precedence spot-checks (gen-first FOR 0.03, storage-first ramprate 0.1); no wrapper leakage into VRE/RSC/hintage files; tech-subset-table minimal membership correct; aggregate_regions preserves clones.

## 3. Latent findings — inactive in current nuclear/TES configs

| ID | Sev | Bug | Where | Trigger |
|----|-----|-----|-------|---------|
| SH-16 | MED | VRE-paired wrappers can't generate: no `cf_tech` → m_cf=0 → GEN_PLANT forced 0; excluded from VRE cc terms (eq_reserve_margin:1752,1759) and eq_curt_gen_balance | d1_temporal_params.gms:568-572; c_supplymodel.gms:1283,1295,3475 | Any upv/wind GSw_StorageHybrid_GenTechs config |
| SH-17 | MED | CAP_ENERGY has no retirement path: `eq_cap_energy_new_noret` strict equality; wrappers retirable under Sw_Retire=3/5 → retired wrapper's TES energy + fom_energy persist forever | c_supplymodel.gms:732-744; b_inputs.gms:6611-6615 | **FIXED 2026-07-25**: eq_cap_energy_new_retub/retmo pair mirrors the power-side retub/retmo (keyed retiretech; noret gated to `not retiretech`); energy retire-penalty (cost_fom_energy·(CAP_ENERGY−INV_ENERGY)) added to objective + e_report replica; no new variables (varfix untouched). Note in-code: m_capacity_exog_energy in retmo permits non-monotone retirement for a future exog+retirable tech — unreachable today (all exog-energy techs are noretire). nlval8 retire_z54: solves optimal with machinery live. **retire_force_z54 (50x TES FOM, RetirePenalty=0): prescription forces 345 MW INV at WY/2032 and the LP retires it AT BIRTH — CAP=0 and CAP_ENERGY=0 same year, zero orphaned energy/FOM in systemcost, 8/8 optimal, checker 49/49 (experience correctly counts gross INV). Path fully exercised** |
| SH-18 | MED | Fossil-paired wrappers bypass gas supply curves (eq_gasused), CAA max-CF; CSAPR NOx priced on net GEN (understates by RTE losses); billed static fuel_price regardless of Sw_GasCurve | c_supplymodel.gms:2970-2982, 2607, 2513/2532; c_supplyobjective.gms:241-246 | gas-cc pairing (documented in switch help) |
| SH-19 | LOW-MED | Min-loading on net GEN not GEN_PLANT (eq_minloading, eq_mingen_lb/ub); wrappers have minloadfrac data (0.2-0.7). **Direction decided 2026-07-24**: net GEN should be minload-free (storage modulates to zero); reactor min load binds GEN_PLANT (as eq_mingen_fixed already does) | c_supplymodel.gms:1503-1511, 1322-1346 | Sw_MinLoading=1 or Sw_Mingen=1 (defaults 0) |
| SH-20 | LOW | PRM sees winter cap swing (ccseason_cap_frac_delta) that dispatch never sees (eq_hybrid_plant_energy_limit has no seas_cap_frac_delta) | c_supplymodel.gms:1785 vs 3477 | **FIXED 2026-07-25**: eq_hybrid_plant_energy_limit dispatchable term now carries (1+Σ h_szn·seas_cap_frac_delta) — dispatch sees the same seasonal swing PRM credits |
| SH-21 | LOW | Wrapper TES outage-free in LP: storage_hybrid branch of eq_storage_capacity drops `avail`; TES can discharge at full bcr·CAP during full-plant outage — inconsistent with gen-first outage coupling in PRAS. **Resolved by 2026-07-24 design principle**: all output flows through the shared powerblock → apply plant `avail` to the wrapper storage branch | c_supplymodel.gms:3115-3116, 3501 | **FIXED 2026-07-25**: wrapper branch of eq_storage_capacity gets avail·(1+seas delta); branches now identical → merged back to the single upstream term |
| SH-22 | LOW | Dangling INV_ENERGY/CAP_ENERGY columns for pvb/CSP (eq_cap_energy_new_noret widened to hybrid_plant; not in objective; not tfix-fixed) — degenerate free columns | c_supplymodel.gms:732; d2_varfix.gms:36 | Cosmetic/solver hygiene |
| SH-23 | LOW | Sw_StartCost=5 bills reactor start costs on TES dispatch swings of net GEN; =1/2 exempt wrapper while standalone nuclear pays. **Direction decided 2026-07-24**: wrappers are storage-like for start/ramp costs — exempt net-GEN swings (reactor runs flat underneath) | b_inputs.gms:5149-5154 | Non-default GSw_StartCost |
| SH-24 | LOW | Reporting-only: costnew/no_credits bill wrapper INV_ENERGY at blended `cost_cap_fin_mult` instead of `_s` (objective + lcoe correct) | e_report.gms:1188-1190, 1345 | Diagnostics only |
| SH-25 | LOW | `process_storages` direct dict indexing → KeyError crash on standalone storage with MW above cutoff but MWh=0 (energy_cap>0 filter drops the key); hybrid path uses get(...,0.0) | reeds2pras/src/utils/reeds_data_parsing.jl:804; prep_data.py:489 | **FIXED 2026-07-25**: get(...,0.0) + @warn mirroring the hybrid path (zero-energy storage inert in PRAS, no crash) |
| SH-26 | LOW | capacity_credit cap_hyb: wrapper missing from bcr map keeps MWh with MW=0 (moot while SH-13 open) | ReEDS_Augur/capacity_credit.py:143-152 | With SH-13 fixed |
| SH-27 | LOW | Wrappers absent from outage_forced_temperature / outage_scheduled_monthly (fallback cloning failed) — wrapper hourly FOR flat 0.03 vs nuclear's temp-shaped mean ~0.04 | inputs_case outage files; outage_rates.py "missing" log lines | **FIXED 2026-07-25** (paired with SH-40 per plan): root cause was the prime-mover→tech-group broadcast (wrappers deliberately not in NUCLEAR group); new broadcast_gen_shapes_to_wrappers() copies the gen tech's shaped profile under the wrapper name before flat-fill. nlval8 val_z54: wrapper FOR temp-shaped mean 0.0400 ≡ gen tech; SOR monthly-shaped 0.07 |

## 4. Verified clean (final review, 2026-07-24)

- SH-09 fix safe: EXTRA_PRESCRIP_ENERGY positive-only → prescribed energy minima still enforced; d2_varfix tfix domain matches. Residual nit: pre-firstyear MW+MWh prescriptions no longer pin energy exactly (slack allows overbuild).
- Objective single-billing (INV blended mult, INV_ENERGY at `_s` once, FOM+fom_energy once, VOM plant/storage split, uranium fuel on GEN_PLANT).
- No PRM double-count (wrapper in storage(i), credited once + sdbin); d2_varfix/d2_unfix cover all six hybrid variables; growth limits, interconnection queues, nuclear/battery mandates, RGGI/state policies, e_powfrac, d3_data_dump, learning cost recomposition.
- Pipeline: lowercase keying end-to-end; ivt extension; tech-subset-table/`STORAGE-HYBRID` column; writecapdat demo concat; runchecks header-only tolerance; flex_geo detection on stortech (correct — flex_geo is the storage side).
- Numerical audit (nlval5 outputs): RTE 0.9900 exact; Σ(IN−out)=losses_ann STORAGE to the MWh; ~1 cycle/day; durations 4-9h ≥ 1.5h floor; ITC 32.5% = capex-weighted gen(29.7%)/TES-battery-clone(38.5%) blend; cap_new_ann 3-yr annualization correct (cap_new_ivrt exact: 345 / 419.2 / 4952.5).

## 5. Validation runs

| Run | Config | Result |
|-----|--------|--------|
| nlval3 (pre-fix) | eo_boak_lo, moderate TES | INVALID: 48.8 GW hybrids on 100%-CF + free TES; NEUE 160 ppm w/ phantom batteries |
| nlval4 (post-sweep) | eo_boak_lo, moderate TES | All optimal; ZERO hybrid builds at real TES pricing (147 GW mandate all standalone); checker 32/32; NEUE 2364 ppm (diagnostic); with-builds PRAS path unexercised |
| nlval5 | nlvaltest4 = + GSw_NuclearDemo=1 (Natrium as storage-hybrid-2-nuclear-smr BCR 0.5 @ p21/2031) + plantchar_tes=tes_ATB_2024_advanced | All optimal (2032 straight optimal); Natrium 345 MW + 1035 MWh built; ~5.4 GW endogenous SMR hybrids @ 2035 (BCR 0.75/1.0, ~18 BAs); mandate 147,000.0 exact @ 2035; checker 33/33 (build-driven learning exercised); NEUE 33.9 ppm; PRAS GeneratorStorage path validated with capacity |
| nlval6 | nlvaltest5 = moderate TES + mckinsey mandate FLOOR (GSw_NuclearCapMandate=1) + demo on; ALL 2026-07-24 fixes active | All optimal (2035 has the known benign unscaled-infeasibility acceptance); checker 33/33; Natrium 345 MW + 1035 MWh (6h chosen at MODERATE pricing); ~1.8 GW endogenous config-4 hybrids (p101 1385 MW/7.1h, p13 416 MW/8.2h); **wrapper opres live** (combo 1036/1063 MWh) with standalone nuclear also providing; RTE = 0.999 exact; mandate floor exact at 2035 (114,500.0; 2032 pre-firstyear by design); wrapper ITC 31.7% vs standalone 29.9%; FOR 0.03 gen-first; 0 emissions; NEUE 34.5 ppm; PRAS: 3 wrapper GeneratorStorages @ 0.999 eff, 0 contamination |
| nlval7 | nlvaltest5 rerun with round-4 active fixes (SH-33/35/36/39/41/42/43/44/45) | All optimal (same benign 2035 acceptance); checker 33/33; batch-tail noise identical to nlval6 (df_capex_init/reeds_to_rev/single_case_plots — pre-existing); Natrium 345 MW + 1035 MWh exact; config-4 hybrids 1727 MW/7.3h; mandate floor exact 114,500.0; wrapper opres 2252 MWh (+7% vs nlval6 — SH-35 lifted the net-GEN cap; combo is the only ortype under GSw_OpRes=2); **PRAS -energy.h5 carries 3 wrapper TES columns (SH-39 validated)**; itc_fractions 112 wrapper rows; cooling-link file clean (0 wrapper rows vs 224); emitrate keeps 4 blessed nuclear-wrapper rows; error_check z 1.2e-4 (noise-level, same class as nlval6's 7.3e-5); NEUE 26.7 ppm |

| nlval8 (4 cases @ z54) | cases_nlvaltest6: **val_z54** (stress-period PRM: CapCredit=0 + StressIterateMax=3 - matches the NREL casefile; TESIsland=1), **retire_z54** (mandate off, Retire=4, NukeNoRetire=0, RetirePenalty=0), **tes_frozen_z54** (TESIsland=0 guard), **retire_force_z54** (+50x TES FOM via tes_ATB_2024_moderate_highfom to force wrapper retirement) | val: 9/9 optimal (incl. one live 2035 stress iteration, NEUE 0.17->0.0017 ppm), checker 49/49 (new TES-island checks live), **SH-27 validated** (wrapper FOR temp-shaped mean 0.0400 = gen tech; SOR monthly-shaped 0.07), **SH-39 validated under iterations** (wrapper SOC columns in stress energy files incl. i1), Natrium exact, mandate floor exact 114,500.0, 4.5 GW endogenous config-4; z 1.0e-3 (first-ever stress-mode run - watch item; CapCredit cases 1.65-1.67e-4). retire: 8/8 optimal, checker 49/49, retirement machinery live (~11.5 GW existing nuclear retires) but prescribed wrapper stayed economic. tes_frozen: checker 49/49 - applied TES _s exactly frozen (886,751) vs learned case per-gen-tech 832,823 (SMR x0.939185) / 872,122 (nuclear x0.983503) - **TESIsland switch exact in both positions**. IMPORTANT z54 lesson: stress-period mode empties rep opres_periods -> operating reserves become stress-hour-only (annual opres_supply ~ 0) - upstream design, flagged for the NREL sweep. Also: one-off load.h5 NaN corruption under 3-way simultaneous launch (retire case, year 2032) - pipeline verified clean serially; fail-fast guard added to hourly_load.py; retire rerun serial |
Caveat on nlval5's hybrid adoption: SH-10 inflates it (SMR wrappers regional-cost-discounted). Re-run after SH-10 fix before quoting numbers.

## 6. Known-suspicious inputs (copied as-is from upstream, flagged for NREL)

- TES financials = CSP clones: 5-yr MACRS, 30-yr eval, and 1-yr construction time (battery-derived).
- TES incentives = battery clones: full 30% standalone-storage ITC + 10% energy-community bonus flowing into wrapper TES capex (observed: wrapper ITC 32.5% vs standalone SMR 29.7%).
- `tes_ms_power_cycle` scalar not restored (referenced by nothing in either repo).

## 7. Next actions

1. [x] SH-10..15, SH-28..32 fix pass + nlval6 validation — DONE, committed d9e1237.
2. [x] Round-4 active set (SH-33/35/36/39/41/42/43/44/45 + user-blessed emitrate) — DONE 2026-07-25, nlval7-validated. Diff adversarially reviewed (1 pvb double-count caught pre-run and fixed). Uncommitted as of nlval7 completion.
3. [x] NREL-casefile set (user-approved plan): SH-17 (CAP_ENERGY retirement), SH-27+SH-40 (outage shaping + Gen_Storage derates, landed together), GSw_NuclearLearning_TESIsland switch (default 1; user decision — the shared turbine island learns with the reactor; TES tanks never learn), SH-20/21/25/47e small batch — DONE 2026-07-25, nlval8-validated (4 z54 cases; checker 49/49 incl. new TES-island end-to-end checks). Casefile cases_nuclearlearning_nrel.csv updated per user: GSw_PRM_CapCredit=0 (stress periods), GSw_PRM_StressIterateMax=3.
4. [ ] **NREL sweep flags**: (a) stress-period mode makes operating reserves stress-hour-only (annual opres ≈ 0 — upstream design; wrapper reserve fixes matter within stress hours); (b) cap `-r` on the 80-case sweep — one-off load.h5 corruption seen under 3-way simultaneous launch (hourly_load now fails fast on NaN); (c) error_check z ≈ 1e-3 in stress mode (vs 1.6e-4 CapCredit) — first stress-mode run, watch item.
5. [ ] Ticket the remaining latent set (SH-16/18/19/22/23/24/26, SH-34, 37/38, SH-46 b-e, SH-47 a-d/f-i) before any VRE/gas/water wrapper configs; confirm growth-penalty basis with NREL.
6. [ ] Commit checkpoint for rounds 4+5 (round-4 fixes + NREL-casefile set). retire_force_z54 CONFIRMED the retirement path (build→retire-at-birth with energy tracking power exactly); all 4 nlval8 cases green — ready to commit on user go-ahead.
